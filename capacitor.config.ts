import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'doctor.alarma',
  appName: 'doctor-alarma',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
